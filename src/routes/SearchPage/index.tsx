import styles from './SearchPage.module.scss';

const SearchPage = () => {
  return (
    <main className={styles.searchPage}>
      <h1>Search Page</h1>
    </main>
  );
};

export default SearchPage;
